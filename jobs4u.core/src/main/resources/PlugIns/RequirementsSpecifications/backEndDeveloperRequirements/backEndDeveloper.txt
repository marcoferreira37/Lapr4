* Requirements
-> "Database connection" as text.
-> "Max retries" as number may be 5.
-> "Retry interval" as number may be ^3.
-> "Allowed IPs" as group may be one of ["127.0.0.1", "192.168.0.1"].
-> "Enable logging" as yes/no may be yes.
-> "Log level" as text may be "info".
-> "Timeout" as number may be 30+.
